<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    <link href="@Url.Content("~/Content/Site.css")" rel="stylesheet" type="text/css" />
    <script src="@Url.Content("~/Scripts/jquery-1.5.1.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/modernizr-1.7.min.js")" type="text/javascript"></script>
    <link href='http://fonts.googleapis.com/css?family=Droid+Sans' rel='stylesheet' type='text/css' />

    @RenderSection("HeaderExtras", false)
</head>

<body>
    <div class="header">
        <div class="header-title-wrapper">
            <span class="title"><a href="@Url.Action("Index", "Home", Model)">Cuatro</a></span><br />
            <span class="subtitle">ASP.NET MVC Web Demo</span>
        </div>
        @Html.Partial("HeaderProfile", ViewData)
    </div>

    <div class="content-wrapper-outer">
        <div class="sidebar">
            <div class="sidebar-header">
                @Html.ActionLink("Endpoints", "Index", "Foursquare", null, null)
            </div>
            <div class="sidebar-section">
                <div class="sidebar-section-title">
                    Users
                </div>
                <div id="div_users_endpoints" class="sidebar-section-content">
                    <ul>
                        <li>
                            @Html.ActionLink("Friend Requests", "Requests", "Foursquare", null, null)
                        </li>
                        <li>
                            @Html.ActionLink("Leaderboard", "Leaderboard", "Foursquare", null, null)
                        </li>
                        @*<li>
                            <a href="@Url.Action("UserSearch", "Foursquare")">Search Users</a>
                        </li>*@

                        <li>
                            @Html.ActionLink("Unlocked Badges", "Badges", "Foursquare", null, null)
                        </li>
                        <li>
                            @Html.ActionLink("Checkins", "Checkins", "Foursquare", null, null)
                        </li>
                        @*<li>
                            <a href="@Url.Action("FriendsList", "Foursquare")">Friends List</a>
                        </li>
                        <li>
                            <a href="@Url.Action("Mayorships", "Foursquare")">Mayorships</a>
                        </li>
                        <li>
                            <a href="@Url.Action("Tips", "Foursquare")">Tips</a>
                        </li>
                        <li>
                            <a href="@Url.Action("Todos", "Foursquare")">Todos</a>
                        </li>
                        <li>
                            <a href="@Url.Action("VenueHistory", "Foursquare")">Venue History</a>
                        </li>*@
                    </ul>
                </div>
                <div class="sidebar-section-title">
                    Venues
                </div>
                <div id="div_venue_endpoints" class="sidebar-section-content">
                    <ul>
                        <li>
                            @Html.ActionLink("Search", "VenueSearch", "Foursquare", null, null)
                        </li>
                        @*<li>
                            @Html.ActionLink("Search", "Requests", "Foursquare", null, null)
                        </li>*@
                    </ul>
                </div>
            </div>
        </div>
        <div class="content-wrapper-inner">
            @if (ViewBag.Title != ""){
                <div class="page-title">
                    @ViewBag.Title
                    @RenderSection("EndpointOptionsInTitle", false)
                </div>
            }
            <div class="content">
                @RenderBody()
            </div>
        </div>
    </div>
</body>
</html>
